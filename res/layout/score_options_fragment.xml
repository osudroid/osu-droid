<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/frg_background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#80000000"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/fullLayout"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:background="@drawable/rounded_rect"
        android:backgroundTint="#1E1E2E"
        android:divider="@drawable/divider"
        android:orientation="vertical"
        android:showDividers="middle">

        <TextView
            android:id="@+id/room_player"
            style="@style/settings_tab_divider"
            android:paddingVertical="12dp"
            android:text="@string/frg_score_menu_title" />

        <Button
            android:id="@+id/exportReplay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@null"
            android:drawableLeft="@drawable/ios_share_24px"
            android:text="@string/frg_score_menu_export_score" />

        <Button
            android:id="@+id/deleteReplay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@null"
            android:drawableLeft="@drawable/delete_24px"
            android:drawableTint="#FFBFBF"
            android:text="@string/menu_deletescore_delete"
            android:textColor="#FFBFBF" />

    </LinearLayout>

</RelativeLayout>